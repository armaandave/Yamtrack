# Spine iOS Current State

## Screens Done

- App shell: Home, Search, Library, Diary, Profile tabs, with Home as the default signed-in tab.
- Home: dark artwork-led landing screen with a compact avatar/title header, backdrop atmosphere from the newest in-progress item, a borderless poster rail with concise progress, and six actor-aware recent activity rows. A bottom “View all activity” action opens a native Activity screen with pull-to-refresh, cursor pagination, retry states, and the same diary/media navigation behavior. The old Friend Feed, People to Follow, and Community Lists placeholders are removed.
- Search: Type Worlds media lens redesigned for Search only, replacing the horizontal segmented type picker with an icon-first lens trigger inside the search field, swipeable lens wheel rail, selected-type label, soft atmospheric bleed, reusable Core/Design components, and persisted lens store ready for later screens.
- Library: dark native library surface rebuilt around paged live tracking results. The screen renders header controls immediately, uses a fixed single-row media type picker, separates Planning items from the main Tracked view, defaults to a four-column poster grid reused from tag pages, supports compact grid/list icon switching, lazy-loads additional tracking pages while scrolling, and opens media detail from every item.
- Media detail: dark production detail page with bleeding poster/accent hero, lowered safe-area-aware poster placement, real provider/Spine rating chips, expandable synopsis, tracked-state summary with progress display, single-path native-keyboard progress updates for in-progress books/games, media-specific metadata, reviews, TV season detail pages with explicit show + season titles and clear episode lists, seasons/episodes, cast/crew/authors, live related sections, poster customization for TMDB movies/TV, and a page-only custom bottom nav overlay. External rating pills open the provider's exact media page when the API supplies a valid URL; game detail renders Steam's percentage and review count with an exact Steam store-page link when available, while unsupported games remain pill-free.
- People: TMDB movie/TV cast and crew rows on media detail can open a native immersive person page when the credit includes a TMDB person id. The person page uses live API data for portrait, biography, optional metadata chips, and a mixed movie/TV filmography poster grid that opens media detail.
- Media log: the media detail LOG action opens a redesigned full-screen native composer with a short, subdued media-detail blur hero, compact poster context, opaque input surfaces, and one type-specific log action (for example, “Log Movie”). The save footer hides while text entry is focused so tag suggestions scroll into view above the keyboard. The screen supports date-only logging, large draggable 5-star half-step ratings, review body, liked/repeat controls, a red on-state spoiler toggle, TV season selection, and progress-only mode for books/manga/comics/games/board games. Dragging the rating all the way left clears it so diary writes omit the rating; tag suggestions are requested only after a non-empty tag query.
- Diary: dark native diary surface using live entries, poster-forward cards, consumed-date fallback formatting, media type chips, ratings, compact tag chips, spoiler/rewatch chips, tappable rows, and a read-only dedicated log detail page with poster/backdrop-led media context, full review display, spoiler reveal, log metadata, navigation to media detail, and tappable tag chips.
- Tags: Profile -> Tags now shows a searchable vertical list of full-width tag pills sorted by usage, with usage counts and no `#` prefix. Diary-log tags open a native tag detail page with a top Diary/Grid segmented control, cleaned-up title text, filtered diary logs, and a unique-media poster grid.
- Poster customization: the media detail ellipsis opens a small bottom menu, then a live poster picker with language filtering, current selection, save, and immediate poster/blur background update.
- Profile: display-first hub rebuilt on the dark native surface with a centered identity hero, compact stat chips, editable Hall of Fame favorite slots, recent diary activity preview, media-detail navigation from favorites/activity, Diary tab jump, and editable settings for profile fields, avatar, preferences, password, import, and logout.
- Stats: Profile -> Stats opens a live, glass-forward personal dashboard with All Time/recent-year pills and the same media lens used by Diary directly below them. All-time consumption is completion-based, so planned/paused/dropped library states are not presented as completed work; year scopes use unique diary-logged titles. The page includes an accessible summary card, all-media rhythm/heatmap, completed media mix, normalized rating distribution, scrollable sparse release-year chart, wrapped genre/language chips with coverage, and neutral three-column Top Rated/Most Logged poster grids that open media detail. Loading, error, empty, retry, pull-to-refresh, stable media selection, 44-point picker targets, and VoiceOver chart descriptors are included.
- Profile Lists: list overview rows are taller media cards with title, item count, and a horizontally scrollable poster preview strip. New and existing lists now use a compact full-screen composer with staged name/description, Public-or-Private visibility, ranked edits, required first media selection, drag ordering for every list, ranked numbering, removal with Undo, discard protection, partial-save retry, and post-create navigation to the finished list. Add Media opens a separate full-screen Search-style picker with debounced live lookup, the media lens, multi-select add/check states, lens-filtered recent media, and a selected-count Done action. The overview keeps the main tab bar visible; list detail remains an immersive pushed screen with its full item grid.

## Live API

- The app always uses live API repositories via `AppRepositories.current()`.
- Authenticated API requests retry once through `POST /api/v1/auth/refresh/` on a 401 when a refresh token exists; tokens are cleared only if refresh is unavailable or fails.
- Library uses paged `GET /api/v1/tracking/?media_type=<type>` results for Tracked, `GET /api/v1/tracking/?media_type=<type>&status=Planning` for Planning, and follows the API `next` page cursor with `page=<cursor>`; no app-shipped cache or fake library data is used.
- Media detail uses `GET /api/v1/media/{source}/{media_type}/{media_id}/` plus `GET /api/v1/tracking/{source}/{media_type}/{media_id}/` for the authoritative saved tracking/progress state when an item is tracked.
- TV season detail uses `GET /api/v1/media/{source}/tv/{media_id}/seasons/{season_number}/` from season cards on a TV show detail page.
- Poster customization uses `GET /api/v1/media/{source}/{media_type}/{media_id}/posters/` and `PUT /api/v1/media/{source}/{media_type}/{media_id}/poster/`.
- Live media detail now consumes normalized `cast`, `crew`, `seasons`, `episodes`, `related_sections`, `external_ratings` (including Steam percentage/review counts and optional provider-page `url` values), `details`, custom posters, backdrop images, and `community.rating_distribution` from the API. Season pages render top-level episode number, title, full date/runtime/rating, overview, and episode image when available.
- People pages use `GET /api/v1/people/tmdb/{person_id}/` through the live `PeopleRepository`. The endpoint is expected to return person profile fields plus mixed movie/TV credits as standard `MediaSummary` objects sorted by TMDB popularity.
- Live review fetch still uses `GET /api/v1/media/{source}/{media_type}/{media_id}/reviews/`; season pages use the TV media path with `season_number` query params and render available review cards.
- Media log uses `POST /api/v1/diary/`, query-driven `GET /api/v1/diary/tags/?q=<text>`, `PATCH /api/v1/tracking/{source}/{media_type}/{media_id}/`, `POST /api/v1/tracking/{source}/{media_type}/{media_id}/actions/consume/`, `POST /api/v1/tracking/{source}/tv/{media_id}/seasons/{season_number}/watch/`, `POST /api/v1/tracking/{source}/book/{media_id}/progress/`, and `POST /api/v1/tracking/{source}/book/{media_id}/complete/`.
- Profile Tags uses `GET /api/v1/diary/tags/?mine=true&all=true` for the full personal tag list. The response is still the existing `{ "results": [...] }` tag-suggestion shape.
- Media detail progress updates use `POST /api/v1/tracking/{source}/book/{media_id}/progress/` for books and `PATCH /api/v1/tracking/{source}/{media_type}/{media_id}/` for game percent progress.
- Diary uses `GET /api/v1/diary/` for the list and `GET /api/v1/diary/{id}/` for row detail; entry creation remains routed through media detail logging.
- Tags use `GET /api/v1/diary/?tag=<tag>` and the existing paged diary response shape. The iOS repository follows diary pages so tag diary/grid views are complete for the API response.
- Profile uses `GET/PATCH /api/v1/me/` for identity, counts, profile fields, preferences, and Hall of Fame slots, `POST/DELETE /api/v1/me/avatar/` for profile photos, `PATCH /api/v1/me/preferences/` for mobile preferences, `POST /api/v1/me/password/` for password changes, `PUT/DELETE /api/v1/me/hof/{media_type}/` for favorite edits, plus `GET /api/v1/diary/` for recent activity.
- Stats uses authenticated `GET /api/v1/stats/me/summary/` with `start_date=all&end_date=all` or inclusive calendar-year dates. The same documented contract is available at `GET /api/v1/users/{username}/stats/summary/`. The UI consumes `completed_count` for all-time totals and per-type mix, `unique_logged_count` for year headlines, and diary-scoped activity/ratings/reviews/repeats/ranked media/release years/genres/languages. `tracked_count` and raw statuses remain decoded for compatibility but never substitute for completion. Mixed native five-star and ten-point rating buckets are normalized onto the chart's ten-point axis, while ranked badges retain their native `/5` or `/10` label. No app-shipped stats fixtures or provider calls are used.
- Profile Lists uses `GET /api/v1/lists/` for overview rows and optional capped `preview_items` poster strips, `GET /api/v1/lists/{id}/` plus paged `GET /api/v1/lists/{id}/items/` for complete edit drafts, `GET /api/v1/meta/` and `GET /api/v1/media/search/` for the live composer picker, `POST/PATCH /api/v1/lists/` for list metadata, `POST/DELETE /api/v1/lists/{id}/items/` for staged membership changes, and `PATCH /api/v1/lists/{id}/items/reorder/` for the complete final order. No backend or shared API contract changes were required.
- Home uses `GET /api/v1/me/`, `GET /api/v1/tracking/?media_type=<type>&status=In%20progress`, and `GET /api/v1/users/{username}/activity/?page_size=6`. The full Activity screen uses the same documented activity endpoint with `page_size=25` and follows its `next_cursor` URL by sending the opaque `cursor` query value. No backend or shared API contract change was required.
- No production-facing fake rating distributions, Goodreads labels, reading progress, author counts, or recommendation placeholders are rendered.
- Movie detail rating chips hide TMDB for now and use bundled IMDb, Letterboxd, and Rotten Tomatoes logo assets when those sources are present.

## Known Gaps

- Poster customization is currently limited to TMDB movies and top-level TV shows; seasons/books/games are not wired in iOS yet.
- Log editing is new-entry only; editing or deleting existing diary entries remains unwired.
- Diary entry detail is wired from Diary rows; public permalink routing and social-like actions are not included yet.
- Tag pages require backend support for exact `tag` filtering on `GET /api/v1/diary/`; the app does not client-filter incomplete diary pages.
- Profile Tags depends on the deployed backend including `all=true` support on `GET /api/v1/diary/tags/`; older backend builds still return the autocomplete-capped 10 results.
- Episode-level logging is not included in the iOS log page yet.
- External ratings, credits, related sections, episodes, seasons, and custom poster URL are typed on iOS; live usefulness depends on provider metadata availability.
- People pages depend on the backend people endpoint being deployed. Non-TMDB credits, books/authors, comics, and provider credits without person ids remain display-only.
- The bottom nav replica on media detail is page-local UI that routes global tab selection through the provided tab callback.
- Home Activity remains the authenticated user's own activity. Switching the actor-aware rows to the documented followed-user `/api/v1/feed/` endpoint and adding social interactions are deferred.
- Stats requires the expanded stats-summary backend in this checkout to be deployed. The endpoint exposes paused/dropped counts but no aggregate factual progress, and the available live rows do not provide usable maxima, so paused and dropped progress is deliberately excluded until the backend supplies normalized completed-unit totals. Likes are an all-time snapshot and are shown only in All Time. Genre/language sections may be partial when local metadata coverage is incomplete. Rating buckets still omit explicit scale metadata, so iOS infers the app's native movie/music/book five-star scale; a future contract field should replace that inference.
- Stats has 16 passing focused iPhone 17 tests. The canonical iPhone 17 build, install, and launch succeed with no build diagnostics. An authenticated live walk-through verified 977 completed titles rather than 2,424 tracked titles, 760 completed Movies, 66 unique titles in 2026, All Media clearing, normalized 7/8 rating bars and 7.5 average, readable/scrollable year labels, native rating denominators, three-column poster grids, and navigation from a poster into Media Detail. The supplied login was used only in Simulator and was not written to the repository.
- Profile Lists poster strips use backend-provided `preview_items` from `GET /api/v1/lists/`; detail pages remain the source for complete list contents.
- List composer unit tests compile, but XCTest execution is currently blocked by the local iPhone 17 simulator test host failing to launch (`NSMachErrorDomain -308`, simulator server died). The normal Spine app builds, installs, and launches successfully. End-to-end authenticated composer verification remains pending because the live production login endpoint returned HTTP 500 in the available simulator session.
- The Codex in-app simulator mirror could not be started during list composer verification because the required `serve-sim` escalation was rejected at the current Codex usage limit; a direct XcodeBuildMCP simulator screenshot and semantic UI snapshot confirmed the live login screen instead.
- Comments and social notifications remain out of scope.
- Library first-page latency now depends on one backend-paginated `/api/v1/tracking/` query. If that page is still slow, the next likely backend work is query profiling/indexing, not iOS skeleton changes.
- Progress display unit preference is stored on-device so Home, Library, Media Detail, and the update sheet keep showing percent when the user saved percent, even if the API later returns equivalent page progress or game progress as minutes. The update progress UI now attaches its panel directly to the native number keyboard with an opaque, content-height accessory background, keeps the unit selector visibly styled, dismisses from outside taps, hides the media detail bottom nav while editing, and presents/dismisses as one connected input surface. Persisting the unit choice across reinstall or another device would require backend contract work; no backend redeploy is needed for this iOS-only fix.
- The redesigned Media Log screen builds and its focused unit tests pass. Simulator launch currently stops at the live login screen without valid API credentials, so final visual inspection of a real media log still requires a working authenticated account and API availability.
- The redesigned Home and paginated Activity screen compile, launch, and all 10 focused Home/activity ViewModel, repository, and timestamp tests pass on iPhone 17. The available simulator reached the live login surface, but `https://api.spine-api.com` returned HTTP 500 during authentication, so authenticated Home/Activity visual verification remains pending API availability and valid stored credentials; the app never substitutes fake Home data.

## How To Run

- Open `ios/Spine/Spine.xcodeproj`.
- Scheme: `Spine`.
- Default Simulator: iPhone 17 (iPhone 16 is not installed on this Mac).
- Default API configuration: `ios/Spine/Spine/Core/Environment/AppConfig.swift`.
- Optional live API override: set `SPINE_API_BASE_URL`.
- XcodeBuildMCP defaults: `.xcodebuildmcp/config.yaml` (project, scheme, simulator, bundle ID).
- Agents with XcodeBuildMCP: use `simulator/build-and-run` after meaningful UI changes; `session_show_defaults` to verify config loaded.
- Build command (fallback):

```bash
DEVELOPER_DIR=/Applications/Xcode-26.5.0.app/Contents/Developer \
  xcodebuild -project ios/Spine/Spine.xcodeproj -scheme Spine \
  -destination 'platform=iOS Simulator,name=iPhone 17' build
```
